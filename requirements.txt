# ============================================================================
# astrbot_plugin_search_pixiv_pic 依赖列表
# ============================================================================
# pixivpy3: Pixiv 非官方 API 封装，用于搜索作品、获取图片 URL
# httpx: 高性能异步 HTTP 客户端，用于下载图片
# Pillow: Python 图像处理库，用于可选的图片格式转换/压缩
# ============================================================================

pixivpy3>=3.0.0
httpx>=0.25.0
Pillow>=10.0.0
